#!/bin/bash

setup-lg() {
    /usr/bin/xrandr --output DP-4 --mode 2560x1440 --output eDP1 --off
}

grep --silent "LG HDR" /var/log/Xorg.0.log

if [ $? == 0 ]; then
    setup-lg
    /home/operator/bin/xsetup
    exit 0
fi
