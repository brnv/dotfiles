#!/bin/bash

set -euo pipefail

if [[ $# == 0 ]]; then
   echo fok you, whats why, beatch
   exit 1
fi


if [[ ! -z $(ip a | grep ppp) ]] && [[ "return" == $1 ]]; then
    marvex-work
else
    marvex -c
fi
